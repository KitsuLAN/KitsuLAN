# 🐳 Infrastructure & Deployment

Конфигурации для запуска проекта в контейнерах.

## Состав
*   **PostgreSQL:** Основная база данных.
*   **Redis:** Брокер сообщений и store для LiveKit.
*   **LiveKit Server:** SFU сервер для аудио/видео.
*   **MinIO:** S3-совместимое хранилище файлов (локально).

## Команды
```bash
# Запустить всё в фоне
docker compose up -d

# Смотреть логи
docker compose logs -f

# Остановить и удалить контейнеры
docker compose down
```

## Конфиги
*   `livekit.yaml` — Настройки **LiveKit** (порты, ключи).